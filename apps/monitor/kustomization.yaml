apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

configMapGenerator:
  - name: prometheus-config
    files:
      - prometheus.yaml

generatorOptions:
  disableNameSuffixHash: true

resources:
  - deployment.yaml
  - ingress.yaml
  - service.yaml
  - pod.yaml
