apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: miniflux

secretGenerator:
  - name: miniflux-secrets
    envs:
      - .env

generatorOptions:
  disableNameSuffixHash: true

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - networkpolicy.yaml
