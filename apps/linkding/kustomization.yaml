apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: linkding

configMapGenerator:
  - name: linkding
    envs:
      - .env

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - networkpolicy.yaml
