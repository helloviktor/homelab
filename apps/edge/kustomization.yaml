apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: edge

secretGenerator:
  - name: tunnel-secrets
    envs:
      - .env

generatorOptions:
  disableNameSuffixHash: true

resources:
  - deployment.yaml
