replicaCount: 1

image:
  repository: miniflux/miniflux
  pullPolicy: IfNotPresent
  tag: ""

nameOverride: ""
fullnameOverride: ""

secret:
  create: false
  name: miniflux-secrets
  envFile: "" # Use --set-file secret.envFile=path/to/.env when secret.create=true

resources:
  requests:
    memory: "64Mi"
    cpu: "250m"
  limits:
    memory: "128Mi"
    cpu: "500m"

service:
  type: ClusterIP
  port: 8080

ingressRoute:
  enabled: true
  host: "" # REQUIRED: Set this to your domain (e.g., "example.com") for IngressRoute to be created
  path: "" # Optional: Set to a path like "/miniflux" or leave empty for host root
  entryPoints:
    - web
  accessLogs: true
  metrics: true
  tracing: true
